#   - do NOT use `export`
#   - do NOT wrap values in quotes  (quotes would become part of the password)
#   - no spaces around `=`
#   - keep the file ASCII-only

DB_USERNAME=root
DB_PASSWORD=your_password_here
# DB_URL=jdbc:mysql://localhost:3307/tilas

# Optional: override the database URL for a specific module.
# If unset, each module falls back to the default in its application.yml.

