<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uestc.mapper.EmpMapper">

    <select id="page" resultType="com.uestc.pojo.Emp">
        select e.*, d.name dept_name from emp e left join dept d on e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">e.name like concat('%', #{name}, '%')</if>
            <if test="gender != null">and e.gender = #{gender}</if>
            <if test="begin != null and end != null">and e.entry_date between #{begin} and #{end}</if>
        </where>
        order by e.update_time desc
    </select>

    <!-- 根据id批量删除员工：foreach拼接成 id in (1,2,3) -->
    <delete id="deleteByIds">
        delete from emp where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <!--
        手动封装：一个员工对应多条工作经历，左外连接后一个员工会出现多行。
        用 resultType 只会得到多个 Emp 对象（经历字段还没地方放），
        所以用 resultMap + collection 把多行里的经历聚合到 Emp.exprList 中。
    -->
    <resultMap id="empResultMap" type="com.uestc.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="image" property="image"/>
        <result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="dept_name" property="deptName"/>

        <!-- 工作经历集合：ofType 指定集合中元素的类型，column 要写 sql 里的别名 -->
        <collection property="exprList" ofType="com.uestc.pojo.EmpExpr">
            <id column="expr_id" property="id"/>
            <result column="emp_id" property="empId"/>
            <result column="begin" property="begin"/>
            <result column="end" property="end"/>
            <result column="company" property="company"/>
            <result column="expr_job" property="job"/>
        </collection>
    </resultMap>

    <!-- 根据id查询员工基本信息及工作信息 -->
    <select id="getById" resultMap="empResultMap">
        select e.*, d.name dept_name,
               ee.id expr_id, ee.emp_id, ee.begin, ee.end, ee.company, ee.job expr_job
        from emp e
                 left join dept d on e.dept_id = d.id
                 left join emp_expr ee on e.id = ee.emp_id
        where e.id = #{id}
    </select>

    <!-- 动态修改：前端只提交要改的字段，为null的字段不参与set，避免把原值覆盖成null -->
    <!-- set标签：自动去掉最后一个逗号 -->
    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <!--
        统计各职位人数：emp.job 存的是 1~5 的整数，库里没有职位字典表，
        所以用 case 直接翻译成职位名，else 兜底成"其他"（job 为 null 或不在 1~5 的值）。
        注意两点：
        1. 别名起成 job_name，不能叫 job，否则和 emp.job 真实列名撞车；
        2. 要按 case 表达式分组（group by job_name），不能 group by job，
           否则 job=null、job=6 各成一组，会出现多个都叫"其他"的行。
    -->
    <select id="countByJob" resultType="com.uestc.pojo.JobCount">
        select case job
                   when 1 then '班主任'
                   when 2 then '讲师'
                   when 3 then '学工主管'
                   when 4 then '教研主管'
                   when 5 then '咨询师'
                   else '其他'
               end      as job_name,
               count(*) as num
        from emp
        group by job_name
        order by num
    </select>

    <!--
        统计男女人数：gender 是 tinyint unsigned not null，1 男 2 女，
    -->
    <select id="countByGender" resultType="java.util.Map">
        select case gender
                   when 1 then '男性员工'
                   when 2 then '女性员工'
               end      as name,
               count(*) as value
        from emp
        group by gender
        order by gender
    </select>

</mapper>
